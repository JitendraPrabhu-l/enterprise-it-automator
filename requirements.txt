mcp>=1.2.0
langgraph>=0.2.60
langgraph-checkpoint-sqlite>=2.0.0
langgraph-checkpoint-postgres>=2.0.0
# langgraph-checkpoint-postgres depends on plain "psycopg", which has no
# working Postgres driver at runtime unless a wrapper is installed (the
# "python" fallback needs a system libpq, which slim container images and
# most dev machines don't have) — pin the binary wheel explicitly rather
# than rely on it being pulled in transitively.
psycopg[binary]>=3.2.0
langchain-core>=0.3.28
langchain-groq>=0.2.3
langchain-anthropic>=0.3.3
langchain-ibm>=1.1.0
langchain-openai>=1.3.0
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
sqlalchemy>=2.0.36
pydantic>=2.10.4
pydantic-settings>=2.7.1
python-dotenv>=1.0.1
aiosqlite>=0.20.0
asyncpg>=0.30.0
httpx>=0.28.1
slowapi>=0.1.9
gunicorn>=23.0.0
opentelemetry-api>=1.29.0
opentelemetry-sdk>=1.29.0
opentelemetry-exporter-otlp-proto-http>=1.29.0
ag-ui-protocol>=0.1.19
prometheus-client>=0.25.0
# App code imports jwt directly (app/api/oidc.py) — pinned explicitly even
# though mcp already pulls it in transitively; [crypto] guarantees the RS256
# backend (cryptography) is present.
PyJWT[crypto]>=2.10.0
alembic>=1.16.0
# Pinned to a pre-release: 1.93.0 (latest stable as of this writing) has no
# published Windows wheel and fails to build from source without a Rust
# toolchain (confirmed against PyPI's release file listing) — 1.94.0rc3 is
# the first release with one. Used ONLY as a local, offline cost-calculator
# utility (litellm.cost_per_token against its bundled static pricing table)
# — NOT as a request gateway/router; this project's own provider-fallback
# logic in app/agent/llm.py is unchanged. Revisit this pin once a stable
# >=1.94.0 ships.
litellm>=1.94.0rc3
# evals/run_reasoning_quality.py's GEval-based judge — a second,
# independent (graded, not exact-match) quality signal alongside
# evals/golden.py's mechanical scoring. Judge model is this project's OWN
# configured LLM (evals/deepeval_judge.py), not OpenAI's default, so this
# adds no new API cost/credential surface.
deepeval>=4.1.9
pytest>=8.3.4
pytest-asyncio>=0.25.1
pytest-cov>=7.0.0
